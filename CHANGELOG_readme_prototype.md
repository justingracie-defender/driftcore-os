# README Update — "This Is a Prototype, Not a Proclamation"

## What changed
- `README.md` — added a new section near the top reframing DriftCore as a
  prototype and an invitation to debate, not a finished decree. Grounds the
  invariants in overlapping consensus (values many worldviews would each
  independently accept), invites critique as a feature, and keeps the hard
  floor justified as "the conditions under which everyone's values can
  coexist."
- The philosophy paper `On_Building_the_Gate_to_the_Garden.docx` is in the
  repo root and is now linked from this section. (If you prefer a
  GitHub-readable version, a .md export can be added later.)

## Nothing else changed
No code touched. Red-team suite still passes 100% (38/38).

## Suggested commit message
```
docs: reframe DriftCore as a prototype open to debate

- Add README section "This Is a Prototype, Not a Proclamation"
- Ground invariants in overlapping consensus, not one person's decree
- Invite critique as a feature; keep the immutable floor justified as
  the precondition for coexisting values
- Link the philosophy paper (On Building the Gate to the Garden)
```

## Reminder
Revoke/rotate the PAT after pushing.
